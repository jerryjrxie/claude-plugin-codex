{
  "name": "claude-plugin-codex",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Use Claude Code from Codex to review code or delegate tasks.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/jerryjrxie/claude-plugin-codex"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "install:local": "bash ./install.sh --personal",
    "install:personal": "bash ./install.sh --personal",
    "install:repo": "bash ./install.sh --repo",
    "test": "node --test tests/*.test.mjs"
  }
}
